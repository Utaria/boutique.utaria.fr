paths:
    migrations: '%%PHINX_CONFIG_DIR%%/db/migrations'
    seeds: '%%PHINX_CONFIG_DIR%%/db/seeds'

environments:
    default_migration_table: phinxlog
    default_database: dev1
    production:
        adapter: mysql
        host: localhost
        name: website
        user: root
        pass: root
        port: 3306
        charset: utf8

    dev1:
        adapter: mysql
        host: localhost
        name: website
        user: root
        pass: root
        port: 3306
        charset: utf8

    dev2:
      adapter: mysql
      host: localhost
      name: website
      user: root
      pass: root
      port: 3307
      charset: utf8

version_order: creation
